default:
  image: ruby:3.2

create-pages:
  script:
    - cd docs
    - gem install bundler
    - bundle install
    - bundle exec jekyll build -d ../public
  pages: true
  only:
    changes:
      - docs/**/*
